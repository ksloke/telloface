Dependencies

Open a anaconda prompt.

1. Install Opencv2
    pip install opencv-python
    pip install opencv-contrib-python

2. Install av library
    pip install av

3. FFmpeg
    Unpack FFmpeg from https://ffmpeg.zeranoe.com/builds/ to a location like "C:\ffmpeg". 
    Add the path to your PATH environmental variables. 